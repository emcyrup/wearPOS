# wearPOS かんたん操作マニュアル（店舗スタッフ向け）

> 最終更新: 2026-09-12 ／ 詳しい機能説明は「wearPOS 機能・運用ガイド」をご覧ください
>
> **PDF 版**: `manual/wearPOS_かんたん操作マニュアル.pdf`
> 文章を直すときは `manual/manual.html` を編集して `npm run manual:build` を実行してください。

このマニュアルは、店頭でよく使う操作だけを順番にまとめたものです。
スマートフォン・タブレット・パソコンのどれでも同じように使えます。

---

## 1. はじめに（画面を開く・ログイン）

ブラウザで **https://wearpos.ai-labo.cloud** を開きます。
よく使う端末では、ブックマークやホーム画面に追加しておくと便利です。

### ログインする

1. 自分の名前を選ぶ（ユーザー名は一覧から選ぶだけです）
2. パスワードを入力して **ログイン**

自分のユーザーがまだ無いときは、**店長（管理者）に作ってもらってください**。
お店の設定によっては、ログイン画面の「新規ユーザーを作成」タブから自分で作れることもあります（合言葉が必要な場合があります）。

⚠️ パスワードを続けて **8 回**間違えると、そのユーザーは **15 分間**ログインできなくなります。

### レジ端末をはじめて使うとき

店頭のレジ端末は、ログインなしでレジ画面を開けるようにできます。
そのかわり、**最初に一度だけ「レジ端末コード」の入力**が必要です。

1. レジ画面を開くと、コードの入力欄が出る
2. 店長から聞いたレジ端末コードを入力して **この端末で使えるようにする**
3. 以降 **90 日間**は、その端末はコードなしでレジを使える

※ ログイン済みであれば、コードを入れなくてもレジは使えます。

### 権限について

- **管理者**: すべての操作（商品登録・価格変更・顧客情報・設定など）
- **スタッフ**: 許可された画面のみ（レジ・商品・在庫など。権限は管理者が決めます）
- 使いたい画面が見えないときは、権限の追加を店長に相談してください
- パスワードを忘れたら管理者に連絡してください（リセットできます）

---

## 2. 会計する（レジ）

サイドメニューの **「レジ ↗」** を押すと、レジ専用ウィンドウが開きます。

1. **担当スタッフを選ぶ**（必須。名札バーコードのスキャンでもOK。選ばないと会計できません）
2. **商品を追加する**
   - 値札のバーコードをスキャン（カメラ or ハンドスキャナ）
   - 読み取れないとき → **「🔍 商品名で検索」**
   - 商品登録がないもの（ノベルティ等）→ **「✏️ 未登録商品を手入力」**
3. **会員をつける**（任意）
   - LINE 会員証バーコードをスキャン、または会員番号を入力して **照会**
   - 分からないとき → **「🔍 お名前・電話番号で検索」**
4. **値引き・ポイントを使う**（任意）
   - 1商品だけ → その行の「この明細を値引き」／会計全体 → 「伝票値引き」
   - ¥ と % の両方を入れると合算されます
5. **支払方法を選ぶ**（現金はお預かり金額を入力するとお釣りが自動計算されます）
6. **「会計する」** を押す
7. **「レシートを印刷」** → 別ウィンドウで開くので印刷して閉じる
8. **「次の会計へ」** で次のお客様へ

在庫の減算・ポイント付与・累計購入額と会員ランクの更新・LINE 通知まで自動で行われます。

### 承認番号を残す（決済メモ）

クレジットや QR 決済など、別の端末で処理した支払いは、**決済端末の承認番号・伝票番号**をメモとして残せます。
入力した番号は伝票詳細に「No. ○○」と表示され、決済端末の控えとの突き合わせに使えます。

---

## 3. 支払いを分けて受け取る

「現金 1万円 ＋ 残りを電子マネー」のように、1 回の会計で支払方法を組み合わせられます。

1. お会計の欄で **「支払方法を分けて支払う」** を押す
2. 使う支払方法のボタンを押す（**残額が自動で入ります**。金額を直せば残りは次の行へ）
3. 現金の行は お預かり金額 を入れる（行ごとにお釣りが計算されます）
4. 内訳の合計がお支払い額とぴったり合ったら **「会計する」**

※ 分割に使えるのは、店長が設定で「分割決済に使える」にした支払方法だけです。
レシートと伝票詳細には内訳が印字・表示され、返品時もこの内訳どおりに返金されます。

---

## 4. 返品する（会計の取り消し）

1. **取引履歴** から対象の伝票を探して開く（伝票番号や会員名で絞り込めます）
2. 右上の **「返品する」** を押す
3. **返す商品の点数**を決める（− ＋ で変更。1点だけ返すこともできます）
4. **返金の方法**を選ぶ
   - そのままなら元の支払い方法どおり
   - 「返金方法を指定する」で、どの支払方法へいくら返すかを指定できます
5. 右下の **「○ 点を返品する」** → 確認画面で OK

### 返品したぶんだけ自動で元に戻るもの

- 商品の在庫（戻し入れ）
- お客様が使ったポイント（返還）・付いたポイント（取消）
- 累計購入額・会員ランク（来店回数は、伝票を全部返品したときだけ 1 減ります）

一部だけ返品したときは、残りをあとから返品できます。伝票には「一部返品」と表示されます。

---

## 5. 商品を登録する（管理者）

**商品 / SKU** → **「商品を登録」**

1. 商品名・価格を入力（必須は **商品名・プロパー価格・サイズ** の3つだけ）
2. 品番は空欄でかまいません（自動採番。例: P2609-001）
3. サイズを選ぶ（カラーは任意。選ばない場合は「指定なし」1つになります）
4. JAN コードは自動採番（採番年月は数字6桁。例: 202609）
5. 登録後、**「値札ラベルを印刷」** でバーコード付きの値札を印刷

### メーカーの値札をそのまま使う（値札の付け替え不要）

バーコードの欄で「既存のバーコードを読み取って登録する」を選ぶと、SKU ごとの読み取り欄が出ます。

- **「📷 まとめて読み取る」** でカメラが開いたままになり、値札を1枚ずつかざすと**上から順に入ります**
- 同じ値札を続けて読んでも二度入りません
- USB / Bluetooth リーダーは、読み取ると**次の空欄へ自動で移ります**
- 同じバーコードを2つの SKU に入れると、その場で警告が出ます
- 読み取り欄は**サイズを選んだあと**に出ます（品番・カラーは未定でも大丈夫）

### たくさんの商品をまとめて登録する（CSV 一括取込）

**商品 / SKU** → **「CSV 一括取込」** → ファイル選択か Excel から貼り付け →
**「内容を確認する」** でプレビュー → **「○ 件を取り込む」**

- 必要な列は **商品名・上代・サイズ** の3つだけ（品番・カラー・JAN・在庫数などは任意）
- 品番が無いときは自動採番し、同じ商品名の行をまとめて1商品にします
- エラーが1件でもあると取り込みません
- 既にある商品は既定では変更せず、JAN が空のところにだけ JAN を入れます

---

## 6. 在庫の作業

**在庫** 画面で行います。一覧のラジオボタンで商品を選ぶと、その SKU がフォームに入ります。

| やりたいこと | 操作 |
| --- | --- |
| 入荷 | 区分「入荷」＋ SKU ＋ 数量 → 登録する |
| 数が合わないとき | 区分「在庫調整」で差分を入力（マイナス可） |
| 棚卸 | 区分「棚卸」で実際に数えた数を入力（上書き） |

すべての増減は「直近の在庫変動」に履歴として残ります。

---

## 7. お客様の登録・LINE

| やりたいこと | 操作 |
| --- | --- |
| 店頭で新規登録 | **顧客 (CRM)** → 顧客を登録（会員番号は自動発行） |
| LINE で自己登録 | 友だち追加 → リッチメニューの「会員登録」 |
| LINE 会員証 | リッチメニューの「会員証」でバーコード表示 → レジでスキャン |
| 既存会員を LINE とつなぐ | 顧客詳細 → 連携コードを発行（6桁） |
| 一斉配信 | 顧客一覧 → LINE 一斉配信 |

登録でお聞きする項目は、お店の設定で変えられます（お名前だけ、ニックネーム可、住所は市区町村まで、など）。
お客様は LINE のトークに「通知オフ」「通知オン」「連携解除」と送ると、自分で設定を変えられます。

---

## 8. 売上を見る・AI に相談する

- **ダッシュボード**: 期間を選んで売上・客数・客単価・売れ筋などを確認
- **AI考察**: 「討論を開始」で、2つの AI が **【考察】**（結論と打ち手）と **【提案】** を出します
- そのまま追加の質問もできます
- **お客様の個人情報は AI に送っていません**（氏名・電話番号・メール・住所・個別の購入履歴は送りません。スタッフ名も「スタッフA」等に置き換えます）
- 「🔍 AIに送るデータを見る」で、送っている内容をその場で全部確認できます

---

## 9. 店長・管理者の設定

サイドメニューの **設定 / 連携** から行います。開店前に一度確認しておくとスムーズです。

| やりたいこと | 場所とポイント |
| --- | --- |
| 店舗名を決める | 設定 → **店舗**。ここで入れた店舗名が**レシートの一番上に印字**されます |
| レジ端末コード | 設定 → **レジ端末のアクセス**。店頭端末をログインなしで使うための合言葉 |
| スタッフを増やす | 設定 → **スタッフ**（レジの担当者）と **ユーザーと権限**（ログインする人） |
| 支払方法を増やす | 設定 → **レジの支払方法**。「分割決済に使える」「お釣りを出せる」もここで設定 |
| 新規ユーザー作成の可否 | 設定 → **ログイン画面での新規ユーザー作成**。既定は「作成させない」 |
| LINE リマインド | 設定 → **リマインド設定**。ON/OFF と日数（毎日 10:00 に自動配信） |
| 顧客登録の項目 | 設定 → **顧客登録の項目**。「最小構成にする」で一括変更 |

⚠️ **データの初期化**は、テスト後に中身を空にするための機能です。既定ではボタンがグレーアウトしています。
実際の運用が始まったら使わないでください。

---

## 10. 困ったときは

| 症状 | 対処 |
| --- | --- |
| バーコードが読めない | レジの「🔍 商品名で検索」から追加。値札が無いものは「✏️ 未登録商品を手入力」 |
| 会計を間違えた | 取引履歴から伝票を開いて「返品する」→ 正しい内容でもう一度会計 |
| 会員が見つからない | レジの「お名前・電話番号で検索」。それでも無ければ新規登録 |
| 会計ボタンが押せない | 担当スタッフが未選択／現金のお預かり金額が不足／分割の内訳が合っていない |
| 「担当スタッフを選んでください」 | レジ画面の担当スタッフ欄が「未指定」のままです |
| 「店舗が設定されていません」 | 店長に連絡（設定 → 店舗 を開いて保存すると直ります） |
| レジでコードを聞かれた | その端末での初回です。店長からレジ端末コードを聞いて入力（1章） |
| ログインが一時的に止まった | パスワードを 8 回続けて間違えると 15 分止まります |
| ポイント残高がおかしい | 顧客詳細のポイント履歴で確認（返品分は自動で調整されています） |
| パスワードを忘れた | 管理者に連絡（設定 → ユーザーと権限 からリセット） |
| 画面が固まった・表示が古い | ブラウザの再読み込み。会計済みのデータが消えることはありません |
