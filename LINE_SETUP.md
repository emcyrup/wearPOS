# wearPOS 公式LINE連携 セットアップ記録

> 最終更新: 2026-08-11 ／ 状態: **設定完了・稼働中**

## 現在の構成

| 項目 | 状態 |
| --- | --- |
| Messaging API チャネル | 作成済み・Webhook 検証成功 |
| Webhook URL | `https://<アプリのドメイン>/api/line/webhook` |
| リッチメニュー | 適用済み（**LIFF 直接遷移モード**） |
| LIFF アプリ | 作成済み（エンドポイント `https://<アプリのドメイン>/liff`） |

## 設定済みの環境変数（Vercel → Settings → Environment Variables）

| 変数名 | 内容 | 取得場所 |
| --- | --- | --- |
| `LINE_CHANNEL_SECRET` | チャネルシークレット | Messaging API チャネル → チャネル基本設定 |
| `LINE_CHANNEL_ACCESS_TOKEN` | チャネルアクセストークン（長期） | Messaging API チャネル → Messaging API 設定 |
| `LIFF_ID` | LIFF アプリの ID（例: `1234567890-abcdefgh`） | LINE Login チャネル → LIFF タブ |
| `LIFF_CHANNEL_ID` | LINE Login チャネルのチャネル ID | LINE Login チャネル → チャネル基本設定 |
| `LINE_PUSH_ENABLED` | `true`（`false` で送信停止・ログのみ） | — |
| `CRON_SECRET` | 自動リマインド実行 API の認証キー | 自分で生成（`openssl rand -base64 32`） |

> **注意**: 環境変数を変更したら必ず **Redeploy**。反映状況はアプリの
> 設定 → LINE公式アカウント連携 の「設定状況」欄で確認できる。

## LINE Developers 側の設定

### Messaging API チャネル
- Webhook URL: `https://<アプリのドメイン>/api/line/webhook`（Webhook の利用: ON）
- **応答メッセージ: OFF**（ON だと LINE の定型文が優先されアプリの返信が届かない）

### LINE Login チャネル（LIFF 用）
- LIFF アプリ: サイズ **Full**／エンドポイント URL `https://<アプリのドメイン>/liff`／スコープ **openid**（+ profile）

## お客様側の体験

1. **友だち追加** → 登録フォームの URL が自動配信
2. **リッチメニュー**（トーク下部・タップで直接画面が開く）
   - 📝 会員登録 → 登録フォーム（登録済みの人は会員証）
   - 💳 会員証 → バーコード付きデジタル会員証（レジでスキャン可）
   - P ポイント → 残高と直近履歴
3. **登録フォーム** で氏名・カナ・電話・メール・誕生日・性別を入力 → 会員番号が自動発行され LINE と連携
4. 以降、**購入通知**（金額・ポイント）／**キーワード応答**（会員証・ポイント・履歴）／
   **自動リマインド**（購入後フォロー・再来店・休眠・誕生日）／**一斉配信** が有効

## 店舗側の運用メニュー

| やること | 場所 |
| --- | --- |
| リッチメニューの再適用 | 設定 → LINE公式アカウント連携 → リッチメニューを適用 |
| 自動リマインドの ON/OFF・日数変更 | 設定 → リマインド設定 |
| リマインドのテスト配信（文面確認・実送信） | 設定 → リマインド設定 → テスト配信（管理者のみ） |
| 顧客ごとのリマインド停止 | 顧客詳細 → このお客様へのリマインド設定 |
| 一斉配信（休眠・ランク別） | 顧客一覧 → LINE 一斉配信 |
| 個別メッセージ（文面自動作成付き） | 顧客詳細 → 個別メッセージを送る |
| 既存会員の LINE 連携（6桁コード） | 顧客詳細 → 連携コードを発行 |
| 会員証が出せないお客様の照会 | レジ → 会員欄「お名前・電話番号で検索」 |

## トラブルシューティング（今回の実績）

| 症状 | 原因 | 対処 |
| --- | --- | --- |
| Webhook 検証が 401 | `LINE_CHANNEL_SECRET` の値がチャネルと不一致（トークンとの取り違え・貼り付けミス・Redeploy 忘れ） | チャネル基本設定のシークレットを貼り直し → Redeploy |
| Webhook URL にアクセスできない | ドメイン（プロジェクト名）の誤り | 普段アプリを開く URL + `/api/line/webhook`。ブラウザで開いて `{"ok":true,...}` が出れば正しい |
| リッチメニューが「キーワード送信方式」になる | `LIFF_ID` / `LIFF_CHANNEL_ID` 未反映 | 環境変数を設定 → Redeploy → 再適用。「タップで直接画面が開きます」と出れば LIFF モード |
| 返信が届かない | LINE 側の応答メッセージが ON | LINE Official Account Manager で応答メッセージを OFF |
| 未連携のお客様がリッチメニューでエラー | LIFF 初回利用時の同意でプロフィール提供に同意していないため、本人確認ができない | 画面の案内どおりトークに「会員登録」と送れば登録できる。恒久対策は LIFF アプリのスコープ `openid` を**必須同意**に設定 |

## 運用上の注意

- 無料プラン（コミュニケーションプラン）は **月200通** まで（こちらから送るプッシュのみカウント。
  お客様の操作への応答はノーカウント）。一斉配信・リマインドを本格運用するなら通数プランを検討
- 自動リマインドは毎日 **10:00 JST** に実行（Vercel Cron）。設定画面から手動実行で事前確認可能
- テスト中に送信を止める場合は `LINE_PUSH_ENABLED=false`（送信ログのみ記録される）
