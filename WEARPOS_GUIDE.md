# wearPOS 機能・運用ガイド

> 最終更新: 2026-08-13 ／ 稼働中（Vercel + Neon PostgreSQL）

アパレル向けの **顧客管理 (CRM) / 在庫管理 / レジ / 売上分析** アプリ。
カラー × サイズの SKU 管理と、シーズンを軸にした値下げ・消化率の把握が中心です。
既存の POS レジとは連携 API でも接続できます。

---

## 画面一覧

| 画面 | できること |
| --- | --- |
| ダッシュボード | 売上・客数・客単価などの実績、期間比較、AI考察 |
| レジ | 店頭での会計（スキャン / 検索 / 手入力 → カート → 支払 → レシート）。**ログイン不要・サイドメニューなしの専用ウィンドウ**（開いていれば既存ウィンドウを再利用） |
| 商品 / SKU | 品番と SKU の一覧・検索（**検索欄でバーコードスキャン可**）、**商品登録**、**商品詳細での項目編集**、値札ラベル印刷 |
| 在庫 | 在庫一覧（**ラジオボタンで選んだ商品をそのまま在庫修正**・行クリックで商品詳細へ）、入荷 / 在庫調整 / 棚卸、変動履歴 |
| 顧客 (CRM) | 会員情報・購買傾向・ポイント・LINE 連携、一斉配信、**顧客の新規登録 / 削除** |
| 取引履歴 | **支払方法別の内訳グラフ**と伝票の一覧（**購入商品を点数・金額つきで全件表示**）・明細、レシート再発行 |
| 設定 / 連携 | リマインド設定、ユーザーと権限、**商品の基本情報 項目**（マスタ管理を含む）、スタッフ管理。POS / LINE 連携の技術情報はページ下部の折りたたみ |

---

## 日常業務の流れ

### 1. 商品を登録する（管理者）

商品 / SKU → **「商品を登録」**

1. 品番・商品名・ブランド / カテゴリ / シーズン・価格・素材を入力
2. **カラーとサイズを選ぶ**と、全組み合わせの SKU が自動生成（件数と一覧をプレビュー）
3. 「JAN コードを自動採番する」で有効な EAN-13 を発行。**採番年月は数字6桁 (例: 202608) で
   直接入力**（📅 でカレンダーからも選択可）。コードは「年月 (YYMM) + 連番5桁」ルールで
   年月ごとに 00001 から自動連番
4. 店舗ごとの初期在庫と発注点を同時に登録（入荷として履歴に残る）
5. 登録後 → **値札ラベル（バーコード）を印刷**してそのまま店頭へ

> 既存商品で JAN が無い場合は、商品詳細の「JANコードを採番」ボタンで後から一括発行できます。

### 2. レジで会計する

レジ画面（**ログイン不要**。店頭のレジ端末でそのまま `/register` を開けます）

| 手順 | 操作 |
| --- | --- |
| 担当者 | プルダウンで選択、または**スタッフ名札バーコードをスキャン**（**必須**。未選択では会計不可）<br>（名札は 設定 → スタッフ → 「名札バーコード」から印刷） |
| 商品を追加 | バーコードをスキャン（カメラ / USB・Bluetooth リーダー / 手入力）<br>読めないときは **「商品名で検索」**（店舗在庫つき・在庫ありが上位）<br>商品登録がないものは **「未登録商品を手入力で追加」**（商品名＋価格を直接入力） |
| 会員をつける | 会員証バーコードをスキャン、会員番号を入力、<br>または **「お名前・電話番号で検索」** |
| 値引き | **明細ごとの値引き**（各行の「この明細を値引き」）と**伝票値引き**。<br>どちらも **¥ と % を併用**でき、合算した額が値引きされます |
| 支払 | ポイント利用を指定 → 支払方法を選択（支払方法は設定で追加・削除できます）<br>**複数の支払方法を併用する場合は「支払方法を分けて支払う」**（例: 現金1万円＋残りを電子マネー） |
| 会計 | **現金**: お預かり金額の入力が必須（不足時は会計不可・不足額を表示）<br>**その他**: 決済端末での完了を確認するダイアログ → 確定 |
| レシート | 会計後に**別ウィンドウ**（サイドメニューなし）で表示して印刷（58mm 想定）。過去の伝票からも再発行可 |

会計すると、**在庫の減算・来店回数・累計購入額・会員ランク・ポイント付与・LINE 購入通知**まで自動で反映されます。

### 3. 顧客に連絡する

| やりたいこと | 場所 |
| --- | --- |
| 個別メッセージ（文面の自動作成つき） | 顧客詳細 → 個別メッセージを送る |
| 休眠会員などへの一斉配信 | 顧客一覧 → **「LINE 一斉配信」ボタン**（モーダルで起動） |
| 自動リマインドの設定 | 設定 → リマインド設定 |
| リマインドのテスト配信 | 設定 → リマインド設定 → テスト配信（管理者） |
| 既存会員の LINE 連携 | 顧客詳細 → 連携コードを発行（6桁） |
| 顧客の新規登録（店頭・電話） | 顧客一覧 → **「顧客を登録」**（会員番号は自動採番） |
| 顧客の削除 | 顧客詳細 → **「顧客を削除」**（購入履歴がある場合は無効化） |
| LINE 連携の解除（店側） | 顧客詳細 → LINE 連携 → **「LINE 連携を解除」** |

---

## 主要機能の詳細

### レジ（店頭会計）
- **ログインなしで利用可能**（店頭端末向け）。担当者は選択またはスタッフ名札バーコードで指定
- スキャン・検索・**未登録商品の手入力**（商品名＋価格。在庫は動かさない）に対応。
  同じ商品を再スキャンで数量加算、± ボタンで調整
- **明細値引き**と**伝票値引き**（どちらも ¥ / % 切り替え）、
  会員のポイント利用（残高と支払額の小さい方が上限）
- 現金は預かり金からお釣りを自動計算
- **分割決済**: 「支払方法を分けて支払う」で、1回の会計に複数の支払方法を組み合わせられます。
  手段ボタンを押すと**残額が自動で入り**、内訳の合計が支払額とぴったり一致するまで会計できません。
  現金など「お釣りを出せる」手段では行ごとに預かり金を入れてお釣りを計算します。
  分割に使えるのは、設定で「分割決済に使える」にした支払方法だけです
- **決済メモ（承認番号）**: 現金以外の支払では、決済端末の承認番号や伝票番号を任意で入力できます。
  分割決済では支払ごとに入力でき、あとから伝票詳細で「No. ○○」として確認できます
- 会計完了画面の「レシートを印刷」は**サイドメニューのない別ウィンドウ**で開く
- 会計は POS 連携 API と同じ処理を通るため、記録の整合性が保たれる

### 商品 / SKU・バーコード
- 品番の配下にカラー × サイズの SKU、カラー×サイズの在庫マトリクス表示
- シーズンごとのプロパー / セール判定、値下げ率と価格改定履歴
- JAN（EAN-13）と Code128 のバーコードを自前生成し、値札ラベルとして印刷
- バーコードは商品登録時に3通りから選べます:
  **自店で自動採番**（「年月 + 連番5桁」ルール）/ **既存のバーコードを読み取って登録**
  （メーカー値札や自店の旧ラベルをそのまま使う。値札の付け替え不要）/ **あとで設定**
- **値札のまとめて読み取り**: 「既存のバーコードを読み取って登録」を選ぶと、SKU ごとの
  読み取り欄が出ます。**「📷 まとめて読み取る」** を押すとカメラが開いたままになり、
  値札を1枚ずつかざすだけで**空いている SKU に上から順に入っていきます**
  （進捗と次に読む SKU を表示。同じ値札を続けて拾ってしまう二度読みは自動で無視します）。
  USB / Bluetooth のバーコードリーダーの場合は、入力欄を選んでそのまま読み取れば
  **読み取りの最後の Enter で次の空欄へ自動で移ります**。
  同じバーコードを2つの SKU に入れてしまったときは、登録前にその場で警告します
- 商品詳細の SKU 一覧から、バーコードを1件ずつ登録・変更・解除できます（管理者）
- **CSV 一括取込**（商品一覧 → 「CSV 一括取込」・管理者）: 仕入先からもらった
  **JAN・上代付きの商品リスト**をそのまま取り込めます。ファイルを選ぶか、Excel からコピーして
  貼り付けるだけで、品番ごとに商品を作り、カラー×サイズの SKU に JAN と在庫を設定します。
  - 使える列: 品番・商品名・ブランド・カテゴリ・シーズン・上代・販売価格・原価・カラー・
    サイズ・JAN・在庫数・素材・原産国（見出しは日本語でも英語でも可。列の順番は自由）
  - 取込前に **新規 / 更新 / 変更なし / エラー** を1行ずつプレビューし、
    ファイル内の SKU・JAN の重複や、既に別の SKU が使っている JAN を教えます。
    エラーが1件でもあると取り込めないので、間違ったまま入ることがありません
  - 「すでにある商品・SKU の内容も上書きする」をオフにすると、新しい品番・SKU だけを追加し、
    **JAN が未設定のものにだけ JAN を入れます**（既存の値札運用を壊しません）
  - 上書きで販売価格が変わったときは、価格改定履歴に残ります
- レジで未登録のバーコードを読み取ると、そのコードを持ったまま商品登録画面を開けます
- **バーコードスキャンは商品一覧の検索欄に組み込み**（読み取った JAN / SKU で絞り込み）
- **SKU コードの変更**: 商品詳細の SKU 一覧で ✎ から変更（管理者。重複チェックあり。
  取引や在庫は内部 ID で紐づくため、過去の履歴はそのまま保たれます）
- ブランド / カテゴリ / シーズンのマスタ管理は 設定 → 商品の基本情報 項目 から

### 在庫
- SKU 単位の在庫と発注点（安全在庫）、安全在庫割れのアラート
  （単店舗運用のため店舗の選択・表示は隠しています）
- **在庫一覧のラジオボタンで商品を選ぶと、その SKU が修正フォームに入る**
  （行クリックは商品詳細への遷移）
- 入荷 / 在庫調整 / 棚卸（棚卸は実棚数で上書き、他は差分）
- すべての変動を履歴に記録（変動後残数のスナップショット付き）

### 顧客 (CRM)
- 会員ランク（レギュラー / シルバー / ゴールド / プラチナ）とポイント付与率の自動判定
- 購買履歴から **よく買うサイズ / カラー** を自動集計
- 休眠顧客（90日以上未来店）の抽出
- **顧客の新規登録**（会員番号は自動採番。LINE からの自己登録と同じ台帳）と**削除**
  （購入履歴がある顧客は記録を守るため無効化）

### 取引履歴
- **支払方法別の内訳**（現金 / クレジット / 電子マネー / QR）を構成比バーとカードで表示。
  絞り込み条件（店舗・期間・キーワード）に連動し、返品を差し引いた正味額で集計
- 売上合計・取引件数・平均客単価のサマリー付き。従来どおりの表形式一覧もそのまま利用可能
- **支払方法別の内訳は支払明細ベース**のため、分割決済でも手段ごとに正しく按分されます
  （一覧の支払方法欄には「現金 + 電子マネー」のように併用した手段を表示）。
  レシートと伝票詳細にも支払の内訳（預かり金・お釣りを含む）を印字します
- **返品（取引の取消・一部返品）**: 伝票詳細の「返品する」で返品画面を開き、
  **明細ごとに返す点数**を決めて返品できます（1点だけ返す、といった一部返品に対応）。
  返品伝票（伝票番号は `元伝票-R`。2回目以降は `-R2`, `-R3`…）を新規作成し、
  **在庫の戻し入れ / 利用ポイントの返還 / 獲得ポイントの取消 / 累計購入額・会員ランクの
  巻き戻し**を返品ぶんだけ自動で行います。来店回数は伝票をすべて返品したときだけ 1 減ります。
  元伝票は書き換えず、返品済みの伝票には「返品済み」「一部返品」と返品伝票へのリンクを表示。
  同じ商品を二重に返品することはできません
- **返金方法の選択**: 既定は**元の支払い方法どおり**（分割決済なら手段ごとに按分して返金）。
  「返金方法を指定する」に切り替えると、**どの支払方法へいくら返すか**を自由に決められます
  （例: クレジットの取消ができないので現金で返す）。
  内訳の合計が返金額とぴったり一致するまで実行できません

### 設定 / マスタ管理（管理者）
- **商品の基本情報 項目のカスタマイズ**: 商品の登録フォームと商品詳細に表示する項目を設定。
  **項目を選択すると、名称・表示/非表示・選択肢・表示順 (↑↓)・削除をまとめて編集**できます。
  並び順は商品登録フォームと商品詳細にそのまま反映されます。
  ブランド / カテゴリ / シーズンは選択肢としてマスタ (追加・削除) をその場で管理、
  素材・原産国・取扱いとカスタム項目（例: フィット、柄）には自由な**選択肢**を
  登録可能（登録するとドロップダウン、空なら自由入力。カラー・サイズは常に表示）
- **レジの支払方法の管理**: 現金・クレジット・電子マネー・QR・その他に加えて、
  **ギフト券・商品券・モール共通ポイントなどを任意に追加・削除**できます。
  支払方法ごとに「レジに表示」「**分割決済に使える**」「お釣りを出せる」を設定でき、
  表示順も変更できます。取引で使用済みの支払方法は、過去の伝票の表示を保つため
  削除せずレジ非表示に切り替わります
- **顧客登録の項目**（設定 → 顧客登録の項目）: 会員登録で集める項目を、項目ごとに
  **必須 / 任意 / 集めない**で選べます。お名前は「姓と名」と「1欄だけ（ニックネーム可）」を
  切り替えられ、住所は「市区町村まで」に制限できます。
  **「最小構成にする」**を押すと、お名前（ニックネーム可）・市区町村・性別・誕生日だけを集め、
  カナ・電話番号・メールは集めない設定になります。
  設定は**店頭の登録画面と LINE の登録フォームの両方**にそのまま反映されます。
  誕生日は残るため、**誕生日リマインドはこれまでどおり使えます**
- **AI考察と個人情報の取り扱い**（設定 → AI考察と個人情報の取り扱い）:
  AI に送っているもの（集計値・スタッフ実績・顧客統計）と、
  **送っていないもの（顧客の氏名・カナ・電話番号・メール・住所・誕生日・個別の購入履歴）**を
  画面に明記しています。スタッフ実績は氏名を「スタッフA」等に置換して送り、画面表示のときだけ
  実名に戻します。**ChatGPT（OpenAI）への送信はチェックひとつで停止**でき、
  オフにすると送信先は Anthropic（Claude）だけになります。
  **「AI接続テスト」**で接続先ごとに「未設定 / 接続OK / エラー」と応答時間を確認でき、
  エラー本文をそのまま表示するため、「IP は使えません」のようなアクセス制限の切り分けができます
- **データの初期化**（設定 → データの初期化）: 取引履歴 / 在庫と変動履歴 / 商品と SKU /
  顧客とポイント / LINE ログ を選んで一括削除できます。件数のプレビュー、確認フレーズの入力、
  実行履歴の記録つき。テストデータを消して本番運用を始めるための機能です。
  **既定では無効（グレーアウト）**で、削除するときだけ画面上部の「有効にする」で解除します。
  1回実行すると自動的に無効へ戻るため、続けて削除するには毎回有効化が必要です
- **スタッフの追加・削除**（取引履歴のあるスタッフは削除の代わりに無効化 / 再有効化）
- **スタッフ名札バーコードの印刷**（レジの担当者スキャン用）

### 売上分析ダッシュボード
- 期間切替（7 / 30 / 90日）＋ カレンダーでの自由な期間指定、前同期間比較
- 日別推移、カラー別 / サイズ別構成、シーズン別、売れ筋 SKU、店舗別・スタッフ別
- **AI考察（Claude × ChatGPT 討論）**: 2つの AI が議論した上で、回答を
  **【考察】（結論と打ち手）と【提案】（一歩先の取り組み）に分けて表示**。
  討論の経過は折りたたみから確認でき、追加質問での壁打ちも同じ形式で回答されます
- **AI に送るのは集計値だけ**: 顧客の氏名・電話番号・メール・住所・個別の購入履歴は送信せず、
  顧客は会員数・新規数・リピート率などの統計のみを送ります。スタッフ名も「スタッフA」等に
  置換して送信します。**「AIに送るデータを見る」**を押すと、実際に送っている内容を
  その場で全文確認できます
- 「表示項目」からユーザーごとに表示セクションをカスタマイズ可能

### LINE 公式アカウント連携
詳細は別紙「**wearPOS 公式LINE連携 セットアップ記録**」を参照。

- 友だち追加 → 登録フォームの URL 自動配信 → フォーム入力で会員登録＆連携
- リッチメニュー（会員登録 / 会員証 / ポイント）をタップで直接画面が開く（LIFF）
- デジタル会員証のバーコードをレジでスキャンして会員照会
- 購入通知、キーワード応答、再来店・おすすめ商品メッセージ、一斉配信
- 自動リマインド（お買い上げ後フォロー / 再来店 / 休眠 / 誕生日）
- お客様側の操作: トークに「**通知オフ / 通知オン**」でお知らせ配信の停止・再開、
  「**連携解除**」で LINE 連携の解除（会員情報とポイントは店に残る）

---

## ログインと権限

- ユーザー名は**選択式**（登録済みユーザーから選んでパスワード入力）
- **ログイン画面の「新規ユーザーを作成」タブ**から、スタッフが自分でユーザーを作って
  そのままログインできます。作成されるのは**スタッフ権限 + 既定機能（レジ・商品・在庫）**のみ。
  管理者は 設定 → **ログイン画面での新規ユーザー作成** で
  「だれでも作成できる / 合言葉が必要 / 作成させない」を切り替えられます
- **管理者**: 全機能。**スタッフ**: 許可された機能のみ（ナビにも出ず、URL 直打ちでもリダイレクト）
- ユーザーの追加・権限変更・パスワードリセット: 設定 → ユーザーと権限（管理者のみ）
- デモ用アカウント: `admin` / `admin1234`（管理者）、`staff` / `staff1234`（レジ・商品・在庫）
- **レジとレシート印刷はログイン不要**（店頭のレジ端末でそのまま使うため）

> **本番運用前に**: デモアカウントのパスワードを変更し、`AUTH_SECRET` にランダム値を設定してください。

### 管理者のみが使える機能
- 商品登録、JAN コード採番
- カテゴリ / スタッフの追加・削除、スタッフ名札バーコードの印刷
- ユーザーと権限の管理
- リマインド設定・テスト配信
- リッチメニューの適用

---

## 環境変数（Vercel → Settings → Environment Variables）

| 変数 | 用途 | 必須 |
| --- | --- | --- |
| `DATABASE_URL` | Neon の **Pooled** 接続文字列 | ✅ |
| `DIRECT_DATABASE_URL` | Neon の **Direct** 接続文字列（マイグレーション用） | ✅ |
| `AUTH_SECRET` | ログインセッションの署名鍵（`openssl rand -base64 32`） | 推奨 |
| `POS_API_KEY` | POS 連携 API の共有シークレット | 外部 POS 連携時 |
| `LINE_CHANNEL_SECRET` / `LINE_CHANNEL_ACCESS_TOKEN` | LINE Messaging API | LINE 利用時 |
| `LIFF_ID` / `LIFF_CHANNEL_ID` | リッチメニューのタップ直接遷移 | LINE 利用時 |
| `CRON_SECRET` | 自動リマインド実行 API の認証 | リマインド利用時 |
| `ANTHROPIC_API_KEY` | AI考察（Claude） | AI考察利用時 |
| `OPENAI_API_KEY` | AI考察の討論相手（ChatGPT） | 討論にする場合 |
| `LINE_PUSH_ENABLED` | `false` で LINE 送信を停止（ログのみ） | 任意 |
| `APP_URL` | LINE で送るリンクの基点 URL | 任意 |

> **重要**: 環境変数を変更したら必ず **Redeploy**（自動では反映されません）。

---

## 運用メモ

| 項目 | 内容 |
| --- | --- |
| デプロイ | GitHub の `main` に push すると Vercel が自動デプロイ（マイグレーションも自動適用） |
| 自動リマインド | 毎日 10:00 JST に実行（Vercel Cron）。設定画面から手動実行で事前確認可 |
| LINE の通数上限 | 無料プランは月200通（こちらから送るプッシュのみカウント。応答はノーカウント） |
| 表示が遅いとき | `/api/health` を2〜3回開いて内訳を確認（コールドスタートの切り分け） |
| 画面デモ | `docs/index.html` に静的デモを生成済み（GitHub Pages で公開可） |

---

## 保護されている範囲

| 対象 | 保護方法 |
| --- | --- |
| 管理画面すべて | ログイン必須 + ロール別の機能制限 |
| POS 連携 API | `X-API-Key`（`POS_API_KEY`） |
| LINE Webhook | チャネルシークレットによる署名検証 |
| 会員証・登録フォーム・ポイント（お客様向け） | ログイン不要だが、LINE ユーザーごとの**署名トークン**でのみ開ける（改ざんは 404） |
| リマインド実行 API | `CRON_SECRET` または `POS_API_KEY` |

---

## 更新履歴

### 2026-08-13 の更新

**返品（取引の取り消し）機能**
- 伝票詳細の「返品する」ボタンで、レジの入力ミス・キャンセル・返品に対応
- 返品伝票（伝票番号 `元伝票-R`）を新規作成し、元伝票は書き換えない
- 在庫の戻し入れ／利用ポイントの返還／獲得ポイントの取消／累計購入額・会員ランク・来店回数の巻き戻しを一括で自動実行
- 返品済みの伝票には「返品済み」表示と返品伝票へのリンク。二重返品は不可
- 売上分析・取引履歴の集計は返品を差し引いた正味額で自動反映
- ※ 明細単位の一部返品と返金方法の選択は 2026-09-06 の更新で対応済み

**SKU コードの変更**
- 商品詳細の SKU 一覧からその場で変更（管理者のみ・重複チェックあり）
- 取引や在庫は内部 ID で紐づくため、過去の履歴はそのまま保たれる

**AI考察の回答形式**
- 回答を【考察】（結論と打ち手）と【提案】（一歩先の取り組み）に分けて表示
- 追加質問（壁打ち）への回答も同じ形式

**レシートの別ウィンドウ化**
- 会計完了画面の「伝票詳細を見る」ボタンを削除
- 「レシートを印刷」はサイドメニューのない別ウィンドウで表示（伝票詳細からの再発行も同様）

**スマートフォン・タブレット対応の強化**
- 全13画面をスマホ幅（390px）・タブレット幅（768px）で検査し、横はみ出しゼロを確認
- 詳細画面ヘッダーのボタンつぶれを修正、レジはタブレットから2カラム表示に
- iOS Safari で入力欄タップ時に画面が勝手にズームされる挙動を防止

**商品情報編集の UI 改善**
- 商品詳細の「✏️ 編集する」ボタンを商品情報カードの右上に配置（編集中は同じ場所がキャンセルに）
- 設定「商品の基本情報 項目」のスマホ表示を整列（項目一覧をグリッド化、入力欄を全幅に）

**新しいドキュメント**
- 「wearPOS かんたん操作マニュアル」（店舗スタッフ向けの簡易マニュアル）を追加
